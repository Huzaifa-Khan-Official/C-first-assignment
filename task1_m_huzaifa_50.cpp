#include <iostream>
using namespace std;

main()
{
	int y,a,x,c;
    cout << "Please give the values of the following: ";
    cout << "\na: ";
    cin >> a;
    cout << "\nx: ";
    cin >> x;
    cout << "\nc: ";
    cin >> c;
    y = a*x + c;
    cout << "The value of y is: " << y;
}
